cat > nixpacks.toml << 'EOF'
[phases.setup]
nixPkgs = ["poppler_utils", "tesseract"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
EOF